# TODO — claude-code-my-workflow

Last updated: 2026-07-01

Project-wide tracker per `.claude/rules/todo-tracking.md`. Active session-tracker tasks (TaskCreate) are working memory; this file is the persistent cross-session record.

## Active (doing now)

- *(nothing actively in flight)*

## Up Next
- [ ] **Context-tightening — remaining levers** (plan `quality_reports/plans/2026-05-28_context-tightening-plan.md`):
  - DONE + MERGED to main 2026-05-28 (merge `448b3d0`): Lever 1 (8 convention rules path-scoped) + low-risk Lever 3 (dropped dead logging requirements; epistemic-table dedup). Always-on rules **165,455 → 116,366 B (−49,089 B / ~12.3k tokens / ~30%)**. Smoke check verified both directions. Branch `audit/workflow-context-tightening` retained (can delete). Not yet pushed to origin.
  - DEFERRED (await approval): Lever 2 (references/ split for data-version-control — held until DVC is used in a live repo) and the hook-rule prose trims (#13–16 in the plan, ~+7%). Lever 3 item E (revision.md diagram, verification↔SSOT overlap) judged not worth it.
- [x] **Evidence-Gating Discipline — COMPLETE + DISTRIBUTED + PUSHED** (design `quality_reports/reviews/2026-05-28_whole-picture-critic-gates-dispatch.md` §7; build plan COMPLETED `quality_reports/plans/2026-05-29_evidence-gating-build-plan.md`). **Phase 1 DONE** (`8176d0f`: normdiff recorder). **Phase 2 DONE** (`03782aa`). **Phase 3 DONE** (`668bc35`). **Phase 4 DONE** (`b96a292`: operationalization gate advisory — requirements-spec Verification-Tier column + workflow.md §1 + Tier-3 panel template). **Phase 5 DONE** (status `quality_reports/reviews/2026-05-30_evidence-gating-distribution-status.md`): merged `b675e83`; Class A propagated to all 7 consumers (hook suite consistent, 0 dangling refs); recorder scope made CLAUDE.md-configurable (`d14b610`, path-traversal-hardened) + propagated; **Class B applied to 2 overlays + 7 consumers** with repo-appropriate `**Analysis roots:**` (tx → `do/`, verified the recorder fires on its incident dir end-to-end) + the operationalization-gate edit. `main` + overlay branches + **all 6 consumer remotes pushed** (va excluded per user — handling its pre-existing `do/main.do` toggles). Minor polish items DONE (`59a3dea`, propagated). **Effort fully closed.**
- [ ] **BDD pre-LFS PDF churn — decide + resolve** (from 2026-06-23 pilot-push session): ~51–57 PDFs under `master_supporting_docs/literature/papers/` read as perpetually "modified" because only 12 of 69 are actual LFS pointers; the rest are pre-LFS binary blobs the active filter wants to convert. Options: `git lfs migrate import` (history rewrite — coauthor coordination), `git add --renormalize` (forward-only conversion), or leave as-is. Overlaps with the previously approved "soft-migrate 60 PDFs to LFS" task.
- [ ] **D6 — pilot exit decision** — now actionable: the DVC pilot push completed 2026-06-23 (blobs in sync, pointer valid, data-doc contract in place). Go/no-go on bulk migration per the five §6.6 metrics in `2026-05-05_lfs-dvc-migration-plan.md`.
- [ ] **`/tools sync-status` implementation script (§7.5)** — the SKILL.md subcommand exists as prose steps (`ad63028`), but the plan's wrapping script (`.claude/skills/tools/sync-status.sh`) was never written. Flagged 2026-06-23 as the one remaining workflow-template gap from the migration plan; it's exactly the tool whose "Pending uploads: N" line would have caught the BDD dangling pointer.
- [ ] **Citation-guard regex hardening — reduce false positives** (added 2026-07-01; user report: the primary-source citation-detection hook fires on false positives too often). One coherent pass over `.claude/hooks/primary_source_lib.py`, folding in the two previously separate backlog items:
  - DONE 2026-07-01 (`aaba644`, mirrors bdm_bic `a68fece`): ADR/workflow status words added to `NEVER_SURNAMES` (`decided`, `proposed`, `superseded`, `supersedes`, `pending`, `resolved`, `updated`, `revised`, `committed`, `delivered`, `drafted`, `status`). Verified: 11 status-word cases no longer extract, 3 real-citation cases unaffected, test suite passes. **Not yet propagated to overlays/consumers** (bdm_bic already has it natively).
  - Remaining blocklist cluster: `Deferred` / `Open` status words; changes-table verbs `Added` / `New` / `Fixed` / `Removed` / `Inserted` / `Replaced` / `Changed` / `Extended` / `Deleted` / `Dropped` / `Copied` / `Merged` / `Patched` followed by date-like strings.
  - Land the unicode-handling fix from the draft proposal `quality_reports/plans/proposals/2026-05-07_primary-source-hook-unicode-fix.md`.
  - Collect fresh false-positive examples from recent sessions before writing the fix, so the pass targets the patterns that actually bite. Add regression tests for every carve-out.
- [ ] **Automate housekeeping passes** (added 2026-07-01) — make housekeeping (updating `TODO.md`, `INDEX.md` files, and other relevant docs after work lands) automatic rather than remembered, and **commit + push after each housekeeping pass**. Design TBD: likely a skill and/or hook; must respect atomic-commit discipline (housekeeping commit separate from substantive work).
- [ ] **Econ academic voice profiles** (added 2026-07-01) — build an economics academic voice from existing well-cited papers, incorporating good writing styles from the field. Likely two profiles: one for **applied micro**, one for **behavioral/experimental**. Should include field-standard terminology and phrasing (e.g. the patterns the anti-AI-prose carve-out item below identifies: "implement an experiment", "serves as the control"). Integration points: `.claude/rules/anti-ai-prose.md` voice profiles, `writer` / `writer-critic` agents, `/humanize`. Sourcing note: profile-building requires reading the exemplar papers (primary-source-first applies).
- [ ] **Atomic commits as default discipline** (added 2026-07-01) — make one-commit-per-logical-change the workflow default. Codify where commit behavior is defined (`/tools commit` skill; possibly a rule file) rather than leaving it session-dependent.

## Waiting On

- [ ] **BDD `data/PROVENANCE.md` backfill** — TBD fields (IRB, fielding dates, PII status, authorized users) need facts only Christina has.
- [ ] **csac2026 year-specific CLAUDE.md fields** — deliverables, status, dataset year; fill when 2026 survey specifics are known.

## Backlog

- [ ] **derive-dont-guess enforcement — overlay + follow-ups** (from 2026-05-28 enforcement work, plan `quality_reports/plans/2026-05-28_derive-dont-guess-enforcement.md`):
  - Apply the CLAUDE.md "Derive, don't guess" Core Principles bullet to `applied-micro` + `behavioral` overlay branches (Class B — manual; folds into the overlay-sync item below).
  - Component 4d (deferred): make `/write` dispatch `writer-critic` so the prose-side derive table reaches the main authoring path.
  - Consider enabling the opt-in block hook (`touch .claude/state/derive-guess-block.enabled`) once the advisory hook's logs show an acceptable false-positive profile.
  - Pre-existing: `test_destructive_action_guard.py` fails pytest collection (unrelated import error surfaced during this work) — investigate separately.
- [ ] **diagnostic-claim enforcement — follow-ups** (from 2026-05-28, plan `quality_reports/plans/2026-05-28_diagnostic-claim-enforcement.md`):
  - Tighten the Stop-audit's evidence check from turn-granularity to the *specific cited file* (v1 passes if any investigation happened this turn, even on a different file).
  - Apply the new CLAUDE.md "Adversarial default" Core Principles bullet to the overlay branches (Class B; folds into the overlay-sync item below).
  - Optional: have critics record `diagnosis:` ledger rows when they confirm/reject a cause, to seed institutional memory.

- [ ] **Quarto (`.qmd`) normalizer for the evidence-gating Tier-1 gate** — deferred from the initial four-language build (Stata/Python/R/LaTeX). Needs `extract_executable_regions` chunk-extraction (YAML + prose + code chunks) *and* broader Quarto infrastructure the repo does not yet have (toolchain, render path, chunk-aware tooling). Pick up alongside the planned Quarto + R migration. See design doc §Q3.
- [ ] **`~/.claude/settings.json` drift** — diverged ~50 days from `claude-config/settings.json` (193 vs 81 bytes). Per `sync-global-config.md`, the live file should be copied to the repo and committed. Small task; ~10 min.
- [ ] **Refine anti-AI-prose catalog: carve out academic-prose false positives.** The humanizer / writer-critic / storyteller-critic currently flag three patterns that are legitimate in academic and experimental-design writing. Skip them in future audits:
  1. **Sentence-start `Importantly,` / `Critically,` / `Notably,`** — standard literature signals, NOT R1 signposting filler. The R1 catalog should distinguish these (academic signal) from `Of course,` / `Clearly,` / `Obviously,` (genuine filler).
  2. **`implement` for experiments** — "implement an experiment / treatment / protocol" is the field-standard verb. L3 "fancy synonyms" rule should NOT rewrite to "run" in experimental contexts.
  3. **`serves as` for role-assignment of treatments/conditions** — "Condition X serves as the control" is field-standard syntax. L4 copula-avoidance rule should NOT rewrite to "is" when the predicate names a role / function / treatment label.

  Update `.claude/rules/anti-ai-prose.md` with the carve-outs (probably as exception notes on R1, L3, L4 catalog rows). Also extend deductions in `agents/writer-critic.md` + `agents/storyteller-critic.md` to honor the carve-outs. ~30 min.
- [ ] **Overlay-branch sync** — applied-micro and behavioral lag main on today's universal-infrastructure commits. Cherry-pick attempt aborted 2026-05-06 due to parallel-history conflicts on `INDEX.md`, `CLAUDE.md`, `tools/SKILL.md`, etc. Properly addressed by the comprehensive propagation plan (above).
- [ ] **Possibly: `/tools sync-overlays` skill** — automate cherry-picking from main onto overlays once the universal-vs-overlay framework is in place. Out of scope until the comprehensive plan settles.
- [ ] **Replication of LFS migration to other research repos** — once BDD pilot exits successfully (D6), bulk-migrate the other research repos: `belief_distortion_discrimination_audit`, `bdm_bic`, `tx_peer_effects_local`, `va_consolidated`, etc. Per `2026-05-05_lfs-dvc-migration-plan.md` §8.
- [ ] **`/tools data-status`** — DVC-only dashboard, separate from `/tools sync-status`. Deferred until BDD pilot reveals whether the catch-all is enough.
- [ ] **Pre-commit hook to block `git push` if `dvc push` is pending** — addresses the "forgot dvc push" failure mode. Per `data-version-control.md` mitigations §3 — listed as "future" because it adds a failure mode (hook fails → push blocked). Revisit if the issue actually bites.

## Done (recent)

- [x] 2026-07-01 — **Lab DVC infrastructure effort CLOSED** — guide ported to `cel_resource_hub` (hub updated by Christina); infrastructure was already built + validated 29/29 in `templates/dvc/` on 2026-06-23. No open items remain on the lab side.
- [x] 2026-06-23 — **DVC pilot push completed** (BDD): dangling pointer closed — re-`dvc add` (263 files, ~1.15 GB), `dvc push` (256 files, cache 0 B → 1.1 GB, in sync), refreshed pointer + `.dvcignore` committed and pushed (`eab28aa`), data-doc contract added (`5f20370`). Session log `2026-06-23_dvc-pilot-push-completion.md`.
- [x] 2026-06-23 — **DVC lab-guide infrastructure built + validated** (`templates/dvc/`): egress guard, sync check, idempotent server setup script, DVC-aware pre-push carve-out; 29/29 end-to-end incl. Scribe simulation. Design doc `quality_reports/dvc-setup-learnings.md`; portable guide draft `quality_reports/dvc-guide-draft.md` (`f8fd139`).
- [x] 2026-06-23 — **csac2026 seeded from applied-micro overlay** (local-only repo, registered in `consumers.toml`, propagate baseline written); overlays brought current first (24 → 35 hooks). Consumer recovery-push audit from 2026-05-31 closed — all 7 recovery commits confirmed on remotes.
- [x] 2026-05-30 — **Evidence-gating minor polish** (`59a3dea`, propagated to 7 consumers): path-likeness guard (non-path first-arg no longer tokenized); reject degenerate declared roots (`.`/`./`/empty → DEFAULT); pytest exit 4 AND 5 → MISSING; configurable `CITATION_TEST_TIMEOUT`; honest block-comment docstring. +3 regression tests (50/50 normdiff, 25/25 citation).

- [x] 2026-05-29 — **Evidence-gating Phase 4 built + committed** (`b96a292`): advisory layer — `requirements-spec.md` Verification-Tier column + `workflow.md` §1 Step-0 operationalization (reminder + critic deduction, never blocks) + `references/evidence-gating-tier3-panel.md`. Docs-only; regression unchanged; review nits resolved.
- [x] 2026-05-29 — **Evidence-gating Phase 3 built + committed** (`668bc35`): Tier-2 — `citation_existence_lib.py` (resolve cited `file:line[:test]` → RESOLVED/MISSING/ASSUMED) + `/tools cite-check` + schema-enforced critic evidence `{claim, artifact_citation, sufficiency_argument}`. Adversarial security lens + independent pen-test (traversal/absolute/shell-injection all rejected, no execution); fixed a Tier-1-deduction wording regression. 24/24 + 36/36.
- [x] 2026-05-29 — **Evidence-gating Phase 2 built + committed** (`03782aa`): the rule (`adversarial-default.md`) + lazy `references/evidence-gating-detail.md` + ledger schema (appended Tier/Citation/Tally, backward-compatible) + **the GATE** (`coder-critic.md` consults the `no-logic-change` ledger row; UNVERIFIED/ASSUMED/missing-row never yield a clean PASS; advisory until Phase 3). Adversarial review caught the gate was under-specified + the ledger append lacked a regression test; fixed + 4 backward-compat tests added (36/36). Independently re-verified incl. recorder↔gate contract.
- [x] 2026-05-29 — **Evidence-gating Phase 1 built + committed** (`8176d0f` on `design/evidence-gating-discipline`): always-on PostToolUse normdiff recorder + 4 language normalizers (Stata/R/Python/LaTeX) + `/tools normdiff` CLI + 32-check test suite; recorder registered in settings.json. Built via workflow with adversarial review — the review caught 4 real correctness bugs (Stata `*` mult-as-comment, set-diff reorder/dup blindness, backtick-macro quote poisoning, seed-as-scaffold) that the build's own tests missed; all fixed + 7 regression tests added; independently re-verified.
- [x] 2026-05-28 — **Dropped dead logging requirements** (context-tightening Lever 3, low-risk subset). Removed `logging.md` §2 (`SESSION_REPORT.md` consolidated master log) AND §3 (`research_journal.md`) — both never instantiated across many sessions; actual practice is per-session logs + `TODO.md`, and the orchestrated agent pipeline (which `research_journal.md` served) isn't run in practice. logging.md 5,445 → 1,972 B (−3,473 B / ~870 tokens off always-on). Cleaned 5 incidental pointers: `file-classes.toml` (dropped SESSION_REPORT pattern), `quality.md` (override → session log), `decision-log.md` (dropped journal bullet), `agents.md` (escalation → session log), `tools/SKILL.md` (removed `/tools journal` subcommand + 2 list mentions). Verified: 0 dangling refs, hooks unaffected, log-reminder.py reference intact.
- [x] 2026-05-28 — **Lever-1 smoke check fully verified** (both directions): fresh session → anti-ai-prose absent; `Read README.md` → harness injected it. Lazy-load confirmed; branch `audit/workflow-context-tightening` ready to merge.
- [x] 2026-05-24 — **Stata block-comment-bug universal fix executed** (3 commits + 1 follow-up fix). Workflow main: `bff2daa` (Commit 1 — rules + critic + skill + field-guide refs + banner-discipline advisory folded in), `2e0851a` (Commit 2 — stata_comment_lib.py + stata_sweep.py + 6 fixtures + test_stata_comment_lib.py + pre-commit template + SKILL.md `/tools stata-sweep` subcommand), `ad8fc61` (Commit 3 — PreToolUse hook stata-comment-balance-check.py + settings.json activation, matcher extended to Edit|Write|MultiEdit), `26cb40c` (follow-up — exclude `.claude` from sweep walk defaults so propagated fixtures don't surface as issues). All 7 consumers + 2 overlays (applied-micro `774224b`/`f08731c`/`9cda7bd` and behavioral `cfc4b63`/`79b265f`/`bcf86fb`) propagated and pushed. Verification: lib unit tests + 5 fixture round-trips PASS, sweep on va_consolidated (129 files) 0 issues idempotent, sweep on BDD (134 files) 0 issues (V7 false positives correctly excluded), sweep on tx_peer_effects_local (297 files) 33 AUTO-FIXABLE + 8 MANUAL-ATTENTION (including `8A_Texas_Heatmaps.do` correctly MA per plan prediction), hook smoke battery 13/13 PASS (V1/V2/V3/V8 block; V7/clean/balanced/legacy-AF/legacy-MA allow). Banner-discipline TODO closed as part of Commit 1 (folded into stata SKILL.md + coder-critic deductions + Comment Safety rule).
- [x] 2026-05-11 — Phase E: CLAUDE.md updated across all 3 branches (Skills Quick Reference row + Cross-Repo Propagation section on main); CHANGELOG entry under [Unreleased] for the comprehensive propagation infrastructure + stata skill migration; bootstrap_manifest.py deleted (throwaway per plan §6.1); related TODOs closed.
- [x] 2026-05-10 — Phase D complete + all 7 consumers pushed to origin (1417c23 / 2b865de / 535b71c / 10d6073 / cab0b7d / 5a92a04 / 287b8df). Workflow main + both overlays + tag `bootstrap-2026-05-10` all pushed. Universe consistent.
- [x] 2026-05-10 — Phase D: bootstrap complete. sync-overlays --force on applied-micro (`0def252`) + behavioral (`4f260b1`); propagate --force-initial on 7 consumers (193 file copies, 0 errors); catch-up sync-overlays --force after the resolve_patterns fix (`67b7437` / `f32f49e`). Idempotency PASS on both. Tag `bootstrap-2026-05-10` (commit `c10e276`).
- [x] 2026-05-07/11 — Comprehensive propagation infrastructure (Phases A–E): file-class manifest, class-aware propagate.py, `/tools sync-overlays`, bootstrap of 2 overlays + 7 consumers (tag `bootstrap-2026-05-10`), CLAUDE.md/CHANGELOG updates. Details in session logs `2026-05-07_propagation-plan-and-stata-skill-migration.md` and git history.
