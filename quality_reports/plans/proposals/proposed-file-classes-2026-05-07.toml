# Proposed .claude/file-classes.toml — autogenerated by bootstrap_manifest.py
# Generated 2026-05-07 from workflow @ 01b157c across branches: main, applied-micro, behavioral.
#
# REVIEW BEFORE COMMITTING TO .claude/file-classes.toml ON MAIN.
#
# Class definitions — see quality_reports/plans/2026-05-07_comprehensive-propagation-plan.md §2.
# Particularly review the [overlay-customized] section: any path the script
# could not auto-classify lands there as 'AMBIGUOUS'. Decide for each:
#   - genuinely customized? leave under [overlay-customized]
#   - just stale-of-main? move to [universal] (and resolve via sync-overlays)

# CLASS A — Universal: byte-identical on every branch where it exists.
# Source-of-truth = main. Default for unlisted files.
# Listed here explicitly for the bootstrap; future Class A files default in.
[universal]
patterns = [
  ".claude/WORKFLOW_QUICK_REF.md",  # byte-identical across all branches
  ".claude/agents/coder-critic.md",  # byte-identical across all branches
  ".claude/agents/coder.md",  # byte-identical across all branches
  ".claude/agents/data-engineer.md",  # byte-identical across all branches
  ".claude/agents/domain-referee.md",  # byte-identical across all branches
  ".claude/agents/editor.md",  # byte-identical across all branches
  ".claude/agents/explorer-critic.md",  # byte-identical across all branches
  ".claude/agents/explorer.md",  # byte-identical across all branches
  ".claude/agents/librarian-critic.md",  # byte-identical across all branches
  ".claude/agents/librarian.md",  # byte-identical across all branches
  ".claude/agents/methods-referee.md",  # byte-identical across all branches
  ".claude/agents/storyteller-critic.md",  # byte-identical across all branches
  ".claude/agents/storyteller.md",  # byte-identical across all branches
  ".claude/agents/tikz-reviewer.md",  # byte-identical across all branches
  ".claude/agents/writer-critic.md",  # byte-identical across all branches
  ".claude/hooks/destructive-action-guard.py",  # byte-identical across all branches
  ".claude/hooks/log-reminder.py",  # byte-identical across all branches
  ".claude/hooks/notify.sh",  # byte-identical across all branches
  ".claude/hooks/post-compact-restore.py",  # byte-identical across all branches
  ".claude/hooks/post-rewrite-verify.py",  # byte-identical across all branches
  ".claude/hooks/pre-compact.py",  # byte-identical across all branches
  ".claude/hooks/primary-source-audit.py",  # byte-identical across all branches
  ".claude/hooks/primary-source-check.py",  # byte-identical across all branches
  ".claude/hooks/primary_source_lib.py",  # byte-identical across all branches
  ".claude/hooks/protect-files.sh",  # byte-identical across all branches
  ".claude/hooks/session-reset.py",  # byte-identical across all branches
  ".claude/hooks/test_destructive_action_guard.py",  # byte-identical across all branches
  ".claude/hooks/test_primary_source_lib.py",  # byte-identical across all branches
  ".claude/hooks/verify-reminder.py",  # byte-identical across all branches
  ".claude/references/content-standards.md",  # byte-identical across all branches
  ".claude/references/domain-profile.md",  # byte-identical across all branches
  ".claude/references/journal-profiles.md",  # byte-identical across all branches
  ".claude/references/pdf-chunking.md",  # byte-identical across all branches
  ".claude/references/replication-standards.md",  # byte-identical across all branches
  ".claude/rules/adversarial-default.md",  # byte-identical across all branches
  ".claude/rules/agents.md",  # byte-identical across all branches
  ".claude/rules/anti-ai-prose.md",  # byte-identical across all branches
  ".claude/rules/data-version-control.md",  # exists only on main; default Universal (no overlay version yet)
  ".claude/rules/decision-log.md",  # byte-identical across all branches
  ".claude/rules/derive-dont-guess.md",  # byte-identical across all branches
  ".claude/rules/destructive-actions.md",  # byte-identical across all branches
  ".claude/rules/exploration-fast-track.md",  # byte-identical across all branches
  ".claude/rules/exploration-folder-protocol.md",  # byte-identical across all branches
  ".claude/rules/figures.md",  # byte-identical across all branches
  ".claude/rules/logging.md",  # byte-identical across all branches
  ".claude/rules/meta-governance.md",  # byte-identical across all branches
  ".claude/rules/no-assumptions.md",  # byte-identical across all branches
  ".claude/rules/output-length.md",  # byte-identical across all branches
  ".claude/rules/primary-source-first.md",  # byte-identical across all branches
  ".claude/rules/python-code-conventions.md",  # byte-identical across all branches
  ".claude/rules/r-code-conventions.md",  # byte-identical across all branches
  ".claude/rules/replication-protocol.md",  # byte-identical across all branches
  ".claude/rules/revision.md",  # byte-identical across all branches
  ".claude/rules/single-source-of-truth.md",  # byte-identical across all branches
  ".claude/rules/tables.md",  # byte-identical across all branches
  ".claude/rules/tikz-visual-quality.md",  # byte-identical across all branches
  ".claude/rules/todo-tracking.md",  # byte-identical across all branches
  ".claude/rules/verification-protocol.md",  # byte-identical across all branches
  ".claude/rules/working-paper-format.md",  # byte-identical across all branches
  ".claude/settings.json",  # byte-identical across all branches
  ".claude/skills/challenge/SKILL.md",  # byte-identical across all branches
  ".claude/skills/commit/SKILL.md",  # byte-identical across all branches
  ".claude/skills/context-status/SKILL.md",  # byte-identical across all branches
  ".claude/skills/deep-audit/SKILL.md",  # byte-identical across all branches
  ".claude/skills/humanize/SKILL.md",  # byte-identical across all branches
  ".claude/skills/learn/SKILL.md",  # byte-identical across all branches
  ".claude/skills/revise/SKILL.md",  # byte-identical across all branches
  ".claude/skills/stata/SKILL.md",  # exists only on main; default Universal (no overlay version yet)
  ".claude/skills/stata/references/doc_lookup.md",  # exists only on main; default Universal (no overlay version yet)
  ".claude/skills/talk/SKILL.md",  # byte-identical across all branches
  ".claude/skills/tools/propagate.py",  # exists only on main; default Universal (no overlay version yet)
  ".claude/skills/write/SKILL.md",  # byte-identical across all branches
  "CHANGELOG.md",  # byte-identical across all branches
  "CONTRIBUTING.md",  # byte-identical across all branches
  "LICENSE",  # byte-identical across all branches
  "README.md",  # byte-identical across all branches
  "docs/README.md",  # byte-identical across all branches
  "docs/concepts/README.md",  # byte-identical across all branches
  "docs/concepts/appropriate-use.md",  # byte-identical across all branches
  "docs/concepts/epistemic-rules.md",  # byte-identical across all branches
  "docs/concepts/quality-scoring.md",  # byte-identical across all branches
  "docs/concepts/upstream-differences.md",  # byte-identical across all branches
  "docs/concepts/verification-ledger.md",  # byte-identical across all branches
  "docs/concepts/worker-critic-pairs.md",  # byte-identical across all branches
  "docs/contributing.md",  # byte-identical across all branches
  "docs/customization/README.md",  # byte-identical across all branches
  "docs/customization/adapting-claude-md.md",  # byte-identical across all branches
  "docs/faq.md",  # byte-identical across all branches
  "docs/getting-started/README.md",  # byte-identical across all branches
  "docs/getting-started/applied-micro.md",  # byte-identical across all branches
  "docs/getting-started/behavioral.md",  # byte-identical across all branches
  "docs/getting-started/branch-model.md",  # byte-identical across all branches
  "docs/getting-started/first-session.md",  # byte-identical across all branches
  "docs/getting-started/installation.md",  # byte-identical across all branches
  "docs/getting-started/prerequisites.md",  # byte-identical across all branches
  "docs/reference/README.md",  # byte-identical across all branches
  "docs/reference/agents.md",  # byte-identical across all branches
  "docs/reference/glossary.md",  # byte-identical across all branches
  "docs/reference/hooks.md",  # byte-identical across all branches
  "docs/reference/rules.md",  # byte-identical across all branches
  "docs/reference/skills.md",  # byte-identical across all branches
  "master_supporting_docs/supporting_papers/.gitkeep",  # byte-identical across all branches
  "templates/archive-readme.md",  # byte-identical across all branches
  "templates/data-CHANGELOG.md",  # exists only on main; default Universal (no overlay version yet)
  "templates/data-MANIFEST.md",  # exists only on main; default Universal (no overlay version yet)
  "templates/data-PROVENANCE.md",  # exists only on main; default Universal (no overlay version yet)
  "templates/exploration-readme.md",  # byte-identical across all branches
  "templates/gitattributes-lfs.txt",  # exists only on main; default Universal (no overlay version yet)
  "templates/quality-report.md",  # byte-identical across all branches
  "templates/requirements-spec.md",  # byte-identical across all branches
  "templates/session-log.md",  # byte-identical across all branches
  "templates/setup-machine.sh",  # exists only on main; default Universal (no overlay version yet)
  "templates/skill-template.md",  # byte-identical across all branches
]

# CLASS B — Overlay-customized: differs per branch by intent.
# Source-of-truth = consumer's overlay. NOT touched by sync-overlays.
# AUTO-PROPOSAL FLAG: every entry below was auto-classified as ambiguous
# (file exists on main + at least one overlay with different content).
# Decide per file: keep here (genuine custom) OR move to [universal] (stale).
[overlay-customized]
patterns = [
  ".claude/agents/orchestrator.md",  # AMBIGUOUS — differs from main on behavioral — user reviews B vs stale-of-main
  ".claude/agents/verifier.md",  # AMBIGUOUS — differs from main on behavioral — user reviews B vs stale-of-main
  ".claude/agents/writer.md",  # AMBIGUOUS — differs from main on behavioral — user reviews B vs stale-of-main
  ".claude/hooks/context-monitor.py",  # AMBIGUOUS — differs from main on applied-micro,behavioral — user reviews B vs stale-of-main
  ".claude/rules/quality.md",  # AMBIGUOUS — differs from main on behavioral — user reviews B vs stale-of-main
  ".claude/rules/stata-code-conventions.md",  # AMBIGUOUS — differs from main on applied-micro,behavioral — user reviews B vs stale-of-main
  ".claude/rules/workflow.md",  # AMBIGUOUS — differs from main on behavioral — user reviews B vs stale-of-main
  ".claude/skills/analyze/SKILL.md",  # AMBIGUOUS — differs from main on behavioral — user reviews B vs stale-of-main
  ".claude/skills/discover/SKILL.md",  # AMBIGUOUS — differs from main on behavioral — user reviews B vs stale-of-main
  ".claude/skills/new-project/SKILL.md",  # AMBIGUOUS — differs from main on behavioral — user reviews B vs stale-of-main
  ".claude/skills/review/SKILL.md",  # AMBIGUOUS — differs from main on behavioral — user reviews B vs stale-of-main
  ".claude/skills/submit/SKILL.md",  # AMBIGUOUS — differs from main on behavioral — user reviews B vs stale-of-main
  ".claude/skills/tools/SKILL.md",  # AMBIGUOUS — differs from main on applied-micro,behavioral — user reviews B vs stale-of-main
  ".gitignore",  # AMBIGUOUS — differs from main on applied-micro,behavioral — user reviews B vs stale-of-main
  "CLAUDE.md",  # AMBIGUOUS — differs from main on applied-micro,behavioral — user reviews B vs stale-of-main
  "master_supporting_docs/literature/papers/README.md",  # AMBIGUOUS — differs from main on applied-micro,behavioral — user reviews B vs stale-of-main
]

# CLASS C — Overlay-only: file exists on exactly one (or both) overlay(s).
# Source-of-truth = the overlay branch where it lives.
[[overlay-only]]
branches = ["applied-micro"]
patterns = [
  ".claude/agents/strategist-critic.md",
  ".claude/agents/strategist.md",
  ".claude/references/domain-profile-applied-micro.md",
  ".claude/references/identification-checklists.md",
  ".claude/references/journal-profiles-applied-micro.md",
  ".claude/rules/air-gapped-workflow.md",
  ".claude/skills/balance/SKILL.md",
  ".claude/skills/event-study/SKILL.md",
  ".claude/skills/strategize/SKILL.md",
]

[[overlay-only]]
branches = ["behavioral"]
patterns = [
  ".claude/agents/designer-critic.md",
  ".claude/agents/designer.md",
  ".claude/agents/otree-specialist.md",
  ".claude/agents/qualtrics-specialist.md",
  ".claude/agents/theorist-critic.md",
  ".claude/agents/theorist.md",
  ".claude/references/domain-profile-behavioral.md",
  ".claude/references/inference-first-checklist.md",
  ".claude/references/qualtrics-patterns.md",
  ".claude/references/seminal-papers-by-subfield.md",
  ".claude/rules/experiment-design-principles.md",
  ".claude/skills/design/SKILL.md",
  ".claude/skills/otree/SKILL.md",
  ".claude/skills/preregister/SKILL.md",
  ".claude/skills/qualtrics/SKILL.md",
  ".claude/skills/theory/SKILL.md",
  "experiments/comprehension/.gitkeep",
  "experiments/designs/.gitkeep",
  "experiments/instructions/.gitkeep",
  "experiments/oTree/.gitkeep",
  "experiments/pilots/.gitkeep",
  "experiments/protocols/.gitkeep",
  "experiments/qualtrics/.gitkeep",
  "theory/.gitkeep",
  "theory/proofs/.gitkeep",
]

# CLASS D — Excluded: never propagates. Project-state files.
[exclude]
# Pattern-based exclusions (matched first, before per-file lookup):
patterns = [
  "quality_reports/*",
  "quality_reports/**",
  "decisions/*",
  "decisions/**",
  "master_supporting_docs/literature/papers/*.pdf",
  "master_supporting_docs/literature/papers/**/*.pdf",
  "master_supporting_docs/literature/reading_notes/*",
  "master_supporting_docs/literature/reading_notes/**",
  ".claude/state/*",
  ".claude/state/**",
  ".dvc/*",
  ".dvc/**",
  "data/*",
  "data/**",
  "scripts/*",
  "scripts/**",
  "paper/*",
  "paper/**",
  "talks/*",
  "talks/**",
  "figures/*",
  "figures/**",
  "tables/*",
  "tables/**",
  "explorations/*",
  "explorations/**",
  "replication/*",
  "replication/**",
  "preambles/*",
  "preambles/**",
  "MEMORY.md",
  "SESSION_REPORT.md",
  "TODO.md",
]

# (For reference: 77 tracked files matched these patterns.)
